{% extends "::base.html.twig" %} 
{% block content %}

{% include 'MyPortfelBundle:My:top.html.twig' %}

<form action="{{ path('operation_update', { 'wallet_id' : wallet_id, 'operation_id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('operation', {'wallet_id' : wallet_id}) }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('operation_delete', { 'wallet_id' : wallet_id, 'operation_id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
<br/><br/>
{% endblock %}