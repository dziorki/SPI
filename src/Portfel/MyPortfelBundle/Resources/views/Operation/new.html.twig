{% extends "::base.html.twig" %} 
{% block content %}

<h1>Operation creation</h1>

<form action="{{ path('operation_create', { 'wallet_id': wallet_id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('operation', { 'wallet_id' : wallet_id }) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}